l = [ 11, 45, 1, 2, 4, 6, 8]
print(l)
# l.append(7)          it adds the no. in last of the list
# l.sort()             it arrange the list in asending order
# l.sort(reverse=True) it arrange the list in descending order
# l.reverse            it reverse the list
# print(l.index(1))    it says the position
# print(l.count(1))    it says the number of occurence
# m=l.copy()           it copy the list and create a new one
# m[0] = 0
# l.insert(1, 899)     it adds the second  number on the index which has the value of first number
m = [900, 1000, 1100]
# k = l + m            we just add two list bro 
# print(k)
# l.extend(m)          it add the number at last of the list just like l.append
print(l)